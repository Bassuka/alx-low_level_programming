nclude "main.h"

/**
 * times_table - prints the 9 times table starting with 0
 *
 * Return: no return
 */
void times_table(void)
{
	int a, b;

	_putchar('0');

	for (a = 0; a <= 9; a++)
	{
		for (b = 0; b <= 9; b++)
