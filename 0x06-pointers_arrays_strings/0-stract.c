#include "main.h"
#include <stdio.h>

/**
 * *_stract : the src string to the dest string
 * Description - overwriting the terminating null byte (\0) at the end of dest
 *  and then adds a terminating null byte
 *
 * @a : the pointer to dest
 * @b : the pointer to src
 * Return : a pointer resulting string dest
 */
char *_stract(char *a, char *b)
{
	return (*a);
}
